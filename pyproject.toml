[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "petitcheval"
version = "0.2.0"
description = "Hierarchical TODO TUI & CLI — workspace → task → step"
requires-python = ">=3.9"

[project.scripts]
petitcheval = "petitcheval.__main__:main"
